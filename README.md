# Usage
```
cd ~/code/$USER
git clone git@github.com:andrerferrer/associations-and-validations-445-live
cd associations-and-validations-445-live
bundle install
stt # Open this folder in Sublime Text
rake db:create
rake db:migrate
rake db:seed
rake console
```

Good Luck, Have Fun!

